/*
 * This is sample code generated by rpcgen.
 * These are only templates and you can use them
 * as a guideline for developing your own functions.
 */

#include "mathService.h"
#include <math.h>

struct Math *
mathoperate_1_svc(struct Math *argp, struct svc_req *rqstp)
{
	static struct Math  result;

	/*
	 * insert server code here
	 */
	 
	 // add by ganlin on 2020/03/09
	 
	 switch (argp -> op){
	 	case ADD:
	 		result.result = argp->arg1 + argp->arg2;
	 		break;
	 	case SUB:
	 		result.result = argp->arg1 + argp->arg2;
	 		break;
	 	case LOG:
	 		result.result = log(argp->arg1) / log(argp->arg2);
	 		break;
	 	default:
	 		break;
	 }
	 result.arg1 = argp->arg1;
	 result.arg2 = argp->arg2;
	 result.op = argp -> op;
	 
	 // add end

	return &result;
}
